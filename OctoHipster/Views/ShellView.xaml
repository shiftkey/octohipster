<UserControl x:Class="OctoHipster.Views.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
          <DataGrid ItemsSource="{Binding Customers}" AutoGenerateColumns="False">
              <DataGrid.Columns>
                <DataGridTextColumn CanUserSort="True" Header="Name" Width="*" Binding="{Binding Name}"/>
                <DataGridTextColumn CanUserSort="True" Header="Company" Binding="{Binding Company}"/>
                <DataGridTextColumn CanUserSort="True" Header="Contact" Binding="{Binding Contact}"/>
                <DataGridTextColumn CanUserSort="True" Header="Date Of Birth" Binding="{Binding DateOfBirth}"/>
            </DataGrid.Columns>
              
          </DataGrid>
        
        
    </Grid>
</UserControl>
